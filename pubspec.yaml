name: strengthlabs_beta
description: "Strength Labs — fitness training tracking app"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  go_router: ^17.2.3
  equatable: ^2.0.5
  dio: ^5.4.0
  flutter_secure_storage: ^10.0.0
  intl: ^0.20.2
  path_provider: ^2.1.2
  excel: ^4.0.6
  share_plus: ^12.0.2
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
